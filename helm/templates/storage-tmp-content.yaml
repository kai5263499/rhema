apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: {{ .Release.Name }}-storage-tmp-content
  name: {{ .Release.Name }}-storage-tmp-content
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

