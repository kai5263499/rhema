package rhema

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("bot", func() {
	It("Should bot responsibly", func() {
		var err error
		Expect(err).To(BeNil())
	})
})
